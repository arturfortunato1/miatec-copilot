"""The six agents. Each is one LangGraph node; each maps to a judging dimension.

Every agent ships a runnable STUB returning canned pt-BR data so the whole loop runs with zero
API keys. Replace incrementally — search the codebase for `TODO(real)`.
"""
